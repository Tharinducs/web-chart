const styles = {
    topButton: {
        borderRadius: 0,
        width: 80,
        zIndex: 1,
        pointerEvents: 'auto'
    },
    topButtonClicked :{
        borderRadius: 0,
        width: 80,
        border: '1px solid #0033CC',
        color :'#0033CC',
        zIndex: 1,
        pointerEvents: 'auto'
    },
    topButtonContainer:{
        textAlign: 'right', 
        padding: 20
    }
}

export default styles;